{
  "name": "shopping-cart",
  "version": "1.0.0",
  "scripts": {
    "start": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
